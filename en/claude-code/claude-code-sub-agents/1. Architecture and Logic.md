---
type: knowledge
created: 2026-03-11
updated: 2026-03-11
tags:
  - claude-code
  - sub-agents
  - architecture
  - data-flow
  - orchestration
aliases:
  - Claude Code Sub-agents Architecture
  - Sub-agents Internal Logic
description: Architecture, data flow, core components, configuration, and hooks system of Claude Code Sub-agents, including built-in agents, custom agent files, permission modes, and memory management.
source: https://docs.anthropic.com/en/docs/claude-code/sub-agents
version: Claude Code 1.0+
---

# Claude Code Sub-agents — Architecture and Logic

## 1. Architecture Model

```mermaid
graph TB
    subgraph "Layer 1: Orchestration"
        CTL["Controller Agent"]
        DISPATCH["Dispatch Engine"]
        COLLECT["Result Collector"]
    end

    subgraph "Layer 2: Agent Definition"
        BUILTIN["Built-in Agents"]
        CUSTOM["Custom Agents<br>agents/*.md"]
        PLUGIN_AG["Plugin Agents"]
    end

    subgraph "Layer 3: Execution"
        SA1["Sub-agent 1<br>(Fresh Context)"]
        SA2["Sub-agent 2<br>(Fresh Context)"]
        SA3["Sub-agent 3<br>(Fresh Context)"]
    end

    CTL --> DISPATCH
    DISPATCH --> BUILTIN & CUSTOM & PLUGIN_AG
    BUILTIN & CUSTOM & PLUGIN_AG --> SA1 & SA2 & SA3
    SA1 & SA2 & SA3 --> COLLECT
    COLLECT --> CTL
```

---

## 2. Data Flow

### 2.1. Sub-agent Invocation

```mermaid
sequenceDiagram
    participant CTL as Controller
    participant DE as Dispatch Engine
    participant SA as Sub-agent
    participant FS as File System

    CTL->>DE: Dispatch task with context
    DE->>DE: Select agent type
    DE->>DE: Prepare prompt + permissions
    DE->>SA: Spawn sub-agent (fresh context)
    SA->>FS: Read/write files
    SA->>SA: Execute task
    SA-->>DE: Return status + result
    DE-->>CTL: Collect result
    CTL->>CTL: Process result, decide next step
```

### 2.2. Controller-Sub-agent Communication

| Direction | Content |
|---|---|
| **Controller → Sub-agent** | Task description, relevant file paths, context, constraints |
| **Sub-agent → Controller** | Status (DONE/BLOCKED/NEEDS_CONTEXT), output summary, file changes |

---

## 3. Agent Types

### 3.1. Built-in Agents

| Agent | Purpose | Permission |
|---|---|---|
| **Task Agent** | General implementation | Full |
| **Read-only Agent** | Analysis, review | Read-only |

### 3.2. Custom Agent Files

```markdown
<!-- agents/security-reviewer.md -->
You are a security code reviewer.

## Focus Areas
- SQL injection vulnerabilities
- Cross-site scripting (XSS)
- Authentication bypass
- Secret/credential exposure
- Input validation

## Process
1. Read all changed files
2. Check each focus area
3. Report findings with severity (Critical/High/Medium/Low)
4. Suggest fixes for Critical and High issues
```

Custom agents are placed in:
- `.claude/agents/` — Project-level
- Platform-specific locations

### 3.3. Plugin Agents

Agents bundled within plugins, accessible via namespace: `/plugin-name:agent-name`

---

## 4. Permission Model

```mermaid
flowchart TD
    A["Sub-agent spawned"] --> B{"Permission mode?"}
    B -->|"Full"| C["Read + Write + Execute"]
    B -->|"Read-only"| D["Read only, no modifications"]
    B -->|"Custom"| E["Specific permissions set by controller"]
```

| Mode | Can Read | Can Write | Can Execute | Use Case |
|---|---|---|---|---|
| **Full** | ✅ | ✅ | ✅ | Implementation tasks |
| **Read-only** | ✅ | ❌ | ❌ | Code review, analysis |
| **Custom** | Configurable | Configurable | Configurable | Specialized tasks |

---

## 5. Status Protocol

Sub-agents communicate their completion status:

```mermaid
flowchart TD
    S["Sub-agent completes work"] --> D["DONE"]
    S --> DC["DONE_WITH_CONCERNS"]
    S --> NC["NEEDS_CONTEXT"]
    S --> BL["BLOCKED"]

    D --> R1["→ Proceed to next step"]
    DC --> R2{"Concerns about correctness?"}
    R2 -->|"Yes"| R2a["→ Address before continuing"]
    R2 -->|"Observations only"| R2b["→ Note and proceed"]
    NC --> R3["→ Provide missing context, re-dispatch"]
    BL --> R4{"Root cause?"}
    R4 -->|"Missing context"| R4a["Re-dispatch with more context"]
    R4 -->|"Reasoning limitation"| R4b["Re-dispatch with more capable model"]
    R4 -->|"Task too large"| R4c["Break task apart"]
    R4 -->|"Plan incorrect"| R4d["Escalate to user"]
```

---

## 6. Memory & Context Management

| Aspect | Behavior |
|---|---|
| **Context per sub-agent** | Fresh, isolated — no bleed from main agent |
| **Context size** | Full model context window (e.g., 200K tokens) |
| **State persistence** | Via file system only (not in-memory) |
| **Result passing** | Controller receives summary, not full context |

---

## 7. Hooks Integration

Sub-agents can trigger and be affected by hooks:

| Hook Event | Sub-agent Behavior |
|---|---|
| **PreToolUse** | Applies to sub-agent tool use (can block) |
| **PostToolUse** | Fires after sub-agent tool use |
| **SubagentStart** | Custom event when sub-agent spawns |
| **SubagentComplete** | Custom event when sub-agent finishes |

---

## 8. Error Handling

| Error | Controller Response |
|---|---|
| Sub-agent BLOCKED | Assess cause → re-dispatch or escalate |
| Sub-agent timeout | Kill and retry with simpler task |
| Sub-agent produces wrong output | Reviewer sub-agent catches → fix loop |
| Sub-agent crashes | Log error, skip or retry |

---

## See Also

- [0. Overview](./0. Overview.md) — Overview and key features
- [2. Playbook](./2. Playbook.md) — Setup, creating agents, troubleshooting
- [3. Decision Guide](./3. Decision Guide.md) — Comparison, use cases, decision matrix
